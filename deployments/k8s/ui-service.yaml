apiVersion: v1
kind: Service
metadata:
  name: vk-ads-ui-service
  namespace: vk-ads-mlops
spec:
  type: NodePort
  selector:
    app: vk-ads-ui
  ports:
    - name: http
      port: 80
      targetPort: 80
      nodePort: 30081