global:
  scrape_interval: 5s
  evaluation_interval: 5s
  external_labels:
    monitor: 'vk-ads-inference'

scrape_configs:
  - job_name: 'api'
    metrics_path: '/metrics'
    scrape_interval: 5s
    scrape_timeout: 3s
    static_configs:
      - targets:
          - 'api:8000'
